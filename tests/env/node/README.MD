# Instant-MeiliSearch Node Demo

## Requirements

The root project `instant-meilisearch` should be built before you can try this demo.

## Launching

Launch the JavaScript file using Node:

```bash
node index.js
```
